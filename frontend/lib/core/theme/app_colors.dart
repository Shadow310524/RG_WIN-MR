import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Healix Brand Colors
  static const Color primary = Color(0xFF0D253F); // Deep Healix Navy
  static const Color primaryLight = Color(0xFF1B3B60);
  static const Color primaryDark = Color(0xFF071727);

  static const Color secondary = Color(0xFF00A896); // Vibrant Healix Teal
  static const Color secondaryLight = Color(0xFFE0F7F5);
  static const Color secondaryDark = Color(0xFF028090);

  // Surface & Neutral Backgrounds
  static const Color background = Color(0xFFF8FAFC); // Slate 50
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF1F5F9); // Slate 100
  static const Color border = Color(0xFFE2E8F0); // Slate 200
  static const Color borderFocus = Color(0xFF00A896);

  // Typography
  static const Color textPrimary = Color(0xFF0F172A); // Slate 900
  static const Color textSecondary = Color(0xFF475569); // Slate 600
  static const Color textMuted = Color(0xFF94A3B8); // Slate 400
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // Status & Feedback
  static const Color success = Color(0xFF10B981); // Emerald
  static const Color successLight = Color(0xFFD1FAE5);
  static const Color warning = Color(0xFFF59E0B); // Amber
  static const Color warningLight = Color(0xFFFEF3C7);
  static const Color error = Color(0xFFEF4444); // Red
  static const Color errorLight = Color(0xFFFEE2E2);
  static const Color info = Color(0xFF0EA5E9); // Sky
  static const Color infoLight = Color(0xFFE0F2FE);

  // Distribution Categories
  static const Color sample = Color(0xFF0EA5E9);
  static const Color promotional = Color(0xFF8B5CF6);
  static const Color freeSupply = Color(0xFF10B981);
  static const Color paidSale = Color(0xFF059669);
  static const Color returnType = Color(0xFFF43F5E);
}
